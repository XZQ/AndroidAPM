# AGENTS.md

## Purpose

This is the repository-local handoff entry for AndroidAPM. Treat the current source tree and executable verification as the source of truth; documentation must be corrected when it disagrees with code.

## Read Order

1. `docs/Android_APM_项目文档.md`
2. `docs/PROJECT_HANDOFF.md`
3. `README.md`
4. `CLAUDE.md`
5. `docs/architecture/00_整体架构.md`
6. The matching module document under `docs/architecture/`
7. `docs/protocol/COLLECTOR_WIRE_V2.md` for collector schema, size, and ACK semantics
8. The sibling `AndroidAPM-Server/docs/云端待建设清单.md` for every external/cloud dependency

## Current Verified Baseline

- Documentation synchronization date: `2026-08-24`
- Branch: `develop`; use `git log --oneline -n 10` for the current tip
- Runtime tip: use `git log --oneline -n 10`; the signed remote-config milestone and docs share one delivery commit
- Build units: `27`
- Composition: `25` root Gradle subprojects (`5` foundation + `15` monitoring + `2` extension + `1` distribution bundle + `apm-sample-app` + non-published `apm-benchmark`) and `2` included builds (`apm-plugin`, `build-logic`)
- Main source files: `165` (`160` Kotlin + `4` C + `1` proto)
- Test/benchmark files: `107`
- Toolchain: Java `17`; Gradle runtime JDK `17+`; Gradle `8.13`, AGP `8.13.2`, Kotlin `2.2.21`
- Android: compileSdk `34`, minSdk `24`, targetSdk `34`; JVM bytecode target `17`
- The root build, both included builds, and the isolated Maven consumer use Java `17` toolchains without rejecting newer Gradle-compatible JDK runtimes; Java and Kotlin compilation targets Java `17` bytecode.
- `settings.gradle.kts` fails before project configuration when Gradle runs on a JVM below `17`; `.java-version` advertises the repository default. `python tools/verify_ci.py` is the platform-neutral client CI entry: it validates the checked-in ABI for `23` root publications plus included `apm-plugin`, checks all `24` expected baselines, then reruns root Android/model tests, included plugin tests, and documentation verification.

Current-tip full test refresh on `2026-07-31` used JDK `17.0.14` with forced task reruns. Root `testDebugUnitTest :apm-model:test --rerun-tasks --no-daemon` passed Android `98` suites / `663` tests and model `5` suites / `51` tests; included `apm-plugin test --rerun-tasks --no-daemon` passed `1` suite / `18` tests. All reports contain zero failures/errors/skips. Root Android plus model therefore establishes the current `103`-suite / `714`-test client baseline, with the plugin reported separately. This supersedes the earlier `701`-test current-tip refresh and the Fourteenth-batch `636`-test root / `682`-test Android-plus-model baseline below; the older entries remain historical evidence for the changes they accompanied.

Behavior-preserving hot-path performance hardening on `2026-08-23` used JDK `17.0.14` and touched `apm-core`, `apm-model`, `apm-storage`, and `apm-uploader` without changing any wire, durable-codec, claim/ACK, at-least-once, or sanitization semantics. PII sanitization now copies maps only when a value changes, memoizes the bounded sensitive-name decision per raw key, and each built-in rule rejects by provable minimum-match length. `Apm.emit` adds a precomputed constant retention base (process name + frozen default context) with a test-proven identical numeric estimate, and `buildEvent` shares the frozen default context when business context is empty. The dispatcher reuses one persist buffer per batch, uses a single-lookup module-occupancy increment, and aggregates without allocating the swallow-path output list. `DynamicEventPolicy` caches the six interpolated keys per `(module, name)` stream with identity verification. Line Protocol escaping is a single pass returning the original reference when clean; the durable codec pre-sizes its buffer, uses prebuilt enum-name lookup maps and precomputed enum-name UTF-8, and skips strict UTF-8 validation for all-ASCII bytes; `stableBatchId` hex-encodes by lookup table; V2 typed-field map entries stream with size-then-write instead of two intermediate buffers; versioned upload splitting moved to the additive public `ApmBatchEnvelopeSerializer.serializeWithinBudget`, which encodes each event once and anchors fixed envelope bytes with a one-event probe (an equivalence test compares split points, batch IDs, and payloads against the historical full-reserialize algorithm). SQLite `pendingCount` now runs an index-covered `COUNT(*)` without the `SUM(LENGTH(payload))` full-BLOB scan, prune merges three same-predicate scans into one `GROUP BY`, capacity trim reuses in-transaction statistics instead of rescanning, batch inserts reuse one `ContentValues`, and `claimPending` decodes payloads after committing the lease transaction with corrupt rows deleted in a second small transaction. `HttpApmUploader` gzip uses an 8 KiB buffer. Focused JDK `17.0.14` checks passed core `29` suites / `213` tests, model `5` suites / `53` tests, storage `6` suites / `38` tests, and uploader `4` suites / `27` tests with zero failures/errors/skips; a same-source root `testDebugUnitTest --no-daemon` then passed `99` Android suites / `669` tests (the prior `98` / `663` baseline plus the new estimator suite and six new regression tests) with zero failures/errors/skips; core/storage/uploader lint report `No issues found`; root `apiCheck` passed for all `23` publications after a manual one-line additive `apm-model` baseline update (`serializeWithinBudget`); benchmark release, its AndroidTest Kotlin compilation, and the sample debug build passed. A deferred candidate remains: adding the `priority DESC, timestamp ASC` index for claim ordering requires a SQLite schema version bump and is intentionally not part of this change. This focused full-Android result does not replace the `2026-07-31` complete `tools/verify_ci.py` gate (plugin tests and forced task reruns were not repeated).

Follow-up hardening on the same `2026-08-23` source state closed that deferred candidate plus two smaller pressure-path items, again under JDK `17.0.14`. The SQLite schema moved v3→v4 with a purely additive migration that only creates `idx_priority_desc_ts(priority DESC, timestamp ASC)`; mixed-direction `ORDER BY priority DESC, timestamp ASC` can use neither a forward nor a backward scan of the existing `(priority ASC, timestamp ASC)` index, so claim/readPending previously fell back to a full scan with a temporary B-tree sort every upload cycle — the new index serves the claim ordering directly and the v2→v4 path still recreates all indexes idempotently. A migration test seeds an exact schema-version-3 database with LOW/HIGH rows, verifies the index exists after upgrade, and proves claim still returns HIGH before older LOW with both rows intact. Pressure-path debug logging now carries a zero-ABI guard: `canSkipDebugLogs(logger)` is internal to `apm-core` and returns true only for the built-in `AndroidApmLogger` with debug output disabled (where `d()` is a provable no-op), so the nine dispatcher drop/admission/isolation/eviction/rate-limit/sampling call sites and two aggregator sites skip template-string construction entirely in production defaults while custom loggers keep receiving every call unchanged — the public `ApmLogger` interface gained no member, so implementations compiled against the old interface cannot break at linkage. `RetryingApmUploader.evictLowerPriority` replaced its `filter + minWithOrNull` intermediate lists with one allocation-free scan selecting the same (priority, timestamp)-minimal victim. Focused checks passed storage `6` suites / `39` tests, uploader `4` suites / `27` tests, and core `29` suites / `213` tests with zero failures/errors/skips, plus `No issues found` lint for all three; root `apiCheck` passed for all `23` publications with no further baseline change, documentation verification passed `45` Markdown files / `61` links, and a same-source root `testDebugUnitTest :apm-model:test --no-daemon` passed `99` Android suites / `670` tests plus model `5` suites / `53` tests, all with zero failures/errors/skips. This focused full-Android result still does not replace the `2026-07-31` complete `tools/verify_ci.py` gate (plugin tests and forced task reruns were not repeated).

A final refinement pass on the same `2026-08-23` source state closed the last audit findings without any public ABI change. `ApmBatchEnvelopeSerializer.serializeWithinBudget` now encodes each event exactly once: per-event encodings are computed a single time and shared between the exact contribution arithmetic and the final envelope writes through the internal `serializePreEncoded` path (the public `serialize` delegates to the same writer, so outputs stay byte-identical and the budget-split equivalence test still passes). Line Protocol rendering writes one pre-sized `StringBuilder` directly instead of building per-segment temporary strings plus a default-capacity `joinToString`; `toSortedText` keeps its sorted-by-key contract, calls each host `toString()` exactly once, and sizes its builder from a character lower bound. `ProtobufSerializer.serializeEvent` pre-sizes its output buffer the same way as the durable codec. The SQLite append-path interval statistics resync now recalibrates only the index-covered `COUNT(*)` row dimension every 64 inserts; the payload-byte cache deliberately no longer triggers a full-table `SUM(LENGTH(payload))` BLOB scan on the dispatcher thread — its drift self-heals through exact own-delete decrements, delete-count-mismatch truth reads, in-transaction capacity recalibration, and `clear` zeroing. Forced `--rerun-tasks --no-daemon` focused runs passed core `29` suites / `213` tests, model `5` / `53`, storage `6` / `39`, and uploader `4` / `27` with zero failures/errors/skips, plus `No issues found` lint and root `apiCheck` for all `23` publications with no baseline change; a same-source root `testDebugUnitTest :apm-model:test --no-daemon` passed `99` Android suites / `670` tests plus model `5` / `53` with zero failures/errors/skips. A complete `python tools/verify_ci.py` attempt on this workstation then passed every step through release-candidate publication (JDK/toolchain checks, `24` ABI baselines, supply-chain metadata, forced-rerun root Android/model tests, included plugin tests) but failed at the isolated-consumer step: its `--refresh-dependencies` revalidation cannot complete because outbound TLS to `dl.google.com` and `repo.maven.apache.org` fails at the handshake in this sandbox (`schannel: failed to receive handshake`; AGP `8.13.2` is fully cached locally). This is an environment network failure, not a passing or failing source baseline; a diagnostic consumer run against the identical candidate repository without `--refresh-dependencies` resolved Bundle and plugin and completed `clean assembleDebug` including ASM transformation. The complete gate must be re-run on networked CI before release. Deliberately not done: a `timestamp DESC` index for the debug-only `readRecent` path (write amplification on every insert for a debug view), hoisting the per-request drain buffer in `HttpApmUploader` to a field (public class, custom hosts may call `uploadBatch` concurrently), and `FileEventStore` streaming changes (bounded 500-row compat path).

An adversarial review of all uncommitted hardening then ran: one external reviewer covered `apm-storage` end to end (claim lease windows, cache accounting, migration paths) and the `apm-core`/`apm-model` halves were self-reviewed with targeted mutation-greps over every downstream consumer. Verdicts: map sharing, estimator saturation ordering, persist-buffer reuse, the log guard (no other main-source `ApmLogger` implementation exists), aggregation emission order, policy-key collision handling, all v4 migration paths, prune/trim arithmetic, ContentValues reuse, and wire byte-identity all held; the Kotlin infix-precedence question in the ASCII fast path resolves as `(toInt() and 0xFF) > 0x7F` (infix `and` binds tighter than `>`, and the alternative parse would not compile). Two real risks and one theoretical hazard were found and fixed: (1) `claimPending` now catches a failure of the second corrupt-row delete transaction so an already-committed lease batch is returned instead of being stranded until lease expiry (old code rolled back atomically; the new fallback leaves corrupt rows leased until the next claim retries deletion); (2) the payload-byte cache now heals on detected external drift — `pendingCount` and the 64-insert interval resync compare the fresh `COUNT(*)` against the exact local cache and run one full `resyncStatistics` only when rows disagree, terminating the stale-low drift cross-instance inserts could otherwise leave while keeping the normal path BLOB-scan-free (a regression test seeds a tight-budget store, has a roomy second store push large payloads past the budget, and proves the first store's next append evicts); (3) the codec's capacity estimate clamps its lower bound so a pathological overflow-sum cannot construct a negative buffer capacity. Storage rose to `6` suites / `40` tests with zero failures/errors/skips after these fixes.

Audit follow-up on `2026-08-24` closed the remaining ownership and cross-instance cache gaps without a public ABI addition. SQLite now pairs the index-covered row count with the persistent `sqlite_sequence` AUTOINCREMENT watermark, checked inside each append write transaction and by `pendingCount`, so another store's small-row delete plus large-row insert cannot hide behind an unchanged count; drift rebuilds row count, payload bytes, and insertion sequence from one SQL snapshot, while the normal worker loop remains BLOB-scan-free. Capacity trim derives its final cache from the transaction's exact database truth rather than subtracting from a possibly stale process-local baseline. Public `PiiSanitizer.sanitize` again isolates clean host-owned maps, while the dispatcher uses an internal sharing path only after queue admission froze ownership; the first changed value is reused so stateful custom rules run once per entry. Sensitive-field and dynamic-policy caches reject identifiers over 128 characters while preserving uncached behavior. Forced focused tests passed core `29` suites / `217` tests and storage `6` suites / `42` tests with zero failures/errors/skips. A same-source root `testDebugUnitTest :apm-model:test --no-daemon` then passed `99` Android suites / `677` tests plus model `5` suites / `53` tests, all with zero failures/errors/skips; core/storage lint, root `apiCheck` for all `23` publications, the exact `24`-baseline verifier, supply-chain metadata verification, documentation verification (`45` Markdown files / `61` links), and `git diff --check` also passed.

Android Studio Sync compatibility on `2026-08-02` now uses exhaustive exact-file coverage instead of waiting for IDE import to expose one missing checksum at a time. The three Sync build roots contain `298` root, `154` `apm-plugin`, and `176` `build-logic` source artifacts, including standard Kotlin `sources`, separately named `gradle813-sources` variants, AndroidX sample sources, Gradle `8.13` sources, and the complete cached Groovy `3.0.22` source family. Cached POM/module metadata gaps were also closed, and previously absent BOM/Groovy components were admitted only after direct official comparison. Local files were cross-checked against Google Maven, Maven Central, Gradle Libs/Plugin Portal, Gradle module-declared SHA-256 values, and the Gradle distribution service; dependency verification remains strict with no new trust rule. Current metadata covers `569` / `239` / `240` / `409` components for root / `apm-plugin` / `build-logic` / isolated consumer. `python tools/verify_supply_chain_metadata.py` and fresh `:prepareKotlinBuildScriptModel --refresh-dependencies --no-daemon` runs for root plus both included builds passed. This focused build-metadata result does not replace the `2026-07-31` full test baseline above.

Privacy/security/protocol-input hardening on the same source passed focused core `28` suites / `207` tests, remote-config `3` / `14`, uploader `4` / `27`, and model `5` / `51`, plus affected lint and root API checks. Durable decode now rejects malformed UTF-8, truncation, impossible pre-allocation counts, and trailing bytes; encode enforces the 2 MiB limit while writing. Remote config bounds bytes/depth/nodes/text and rejects duplicate keys before canonicalization, while Ed25519 inputs use canonical Base64 and exact key/signature lengths. PII covers normalized auth variants without treating `author` as sensitive. V2 ACK integers are canonical positive decimals, and `Retry-After` seconds saturate before worker clamping. Fixed-seed mutation corpora cover these boundaries. The complete `tools/verify_ci.py` gate also passed checksum policy, `41` benchmark/device-lab host tests, release-candidate publication, and isolated consumer resolution. This is deterministic regression fuzz evidence, not continuous randomized fuzzing or a substitute for production TLS/proxy and third-party penetration testing.

Current-tip public API checks on `2026-07-31` use Kotlin binary-compatibility-validator `0.18.1`. Root `apiCheck` covers all `23` published root artifacts and the included `apm-plugin` runs its own `apiCheck`; `tools/verify_api_baselines.py` requires exactly `24` checked-in dumps, `23` non-empty public surfaces, and the intentionally empty classless `apm-bundle` dump. `apm-sample-app` and non-published `apm-benchmark` are excluded. The expanded `python tools/verify_ci.py` passed end to end under JDK `17.0.14`, including `98` Android suites / `663` tests, `5` model suites / `51` tests, `1` plugin suite / `18` tests, and documentation verification for `45` Markdown files / `61` links. Baseline updates are manual and governed by `docs/API_COMPATIBILITY.md`; CI never rewrites them.

Host-integration readiness diagnostics on `2026-07-31` added a file-free `ApmDiagnostics.hostIntegrationSnapshot()` for Network, SQLite, IPC, WebView, thread-pool, Battery, and IO integration evidence. Fixed atomic slots separate module activity, exact live registration counts, session observation counts, and latest wall time without retaining host objects or business values. `NO_RUNTIME_EVIDENCE` deliberately means that the relevant operation has not run in this init/stop session, not that static wiring is proven absent. Focused JDK `17.0.14` tests across core plus all seven owning modules passed `49` suites / `359` tests with zero failures/errors/skips; their lint and the additive `apm-core` API check passed. The current full gate is the `663`-test Android baseline above.

Critical-handoff fault injection on `2026-07-31` corrected the remote-process durability boundary without changing public ABI. The uploader process now sends CRITICAL IPC events directly through `dispatchCriticalSync`; a ready file is deleted only after downstream acceptance, while false, recoverable storage failure, or a missing consumer retains the complete file for at-least-once retry. Published `.ipc` files are not age-deleted before acceptance; the `16 MiB` directory budget remains the space bound, and only incomplete `.tmp` artifacts use the five-minute cleanup. Crash hand-off now reports false/recoverable failure while invoking the original uncaught-exception handler exactly once from `finally`; fatal VM errors still propagate after delegation. Focused core/crash/ANR/storage checks passed `43` suites / `284` tests plus lint and root API checks; a real SQLite test closes and reopens the store before recovering the exact CRITICAL identity, priority, and fields. The current full gate is the `663`-test Android baseline above.

Collector V2 cross-repository verification on `2026-07-31` used JDK `17.0.14` and the independent AndroidAPM-Server branch `codex/collector-v2-e2e` at verified tip `2feb2f5`. `python tools/verify_collector_e2e.py --server-repo <checkout>` built the real `HttpApmUploader`, launched the real FastAPI/uvicorn Gateway, sent and replayed the same two-event Gzip V2 envelope, required exact schema/batch/count ACKs, and inspected test-only SQLite persistence. The result retained two unique event IDs, all 12 scalar discriminators, JSON-safe `NaN/Infinity` text, fixed resource, and V2 metadata; the temporary ingest key is passed only via an environment variable and is not printed. The server passed `57` tests plus ruff/mypy/docs. Docker was unavailable, so this evidence must not be extrapolated to PostgreSQL concurrency/durability, TLS/Compose, proxy ACK loss, SigNoz, or production deployment.

Dispatcher performance hardening on `2026-07-31` removed full-candidate `filter + sortedBy` work from pressured HIGH/CRITICAL admission. Replacement now makes fixed LOW→NORMAL→HIGH FIFO passes and allocates only the final victim list after proving enough count/byte capacity; the default aggregation-disabled worker path no longer creates one singleton list per event, and the common non-durable success path does not create an empty rejected-ID set. A mixed-priority regression test preserves LOW-first and oldest-first semantics. `:apm-core:testDebugUnitTest :apm-core:lintDebug :apm-benchmark:assembleRelease :apm-benchmark:compileReleaseAndroidTestKotlin --rerun-tasks --no-daemon` passed core `28` suites / `205` tests with zero failures/errors/skips and `No Issues Found`; all `41` benchmark/device-lab host tests passed. The current complete `tools/verify_ci.py` gate passed Android `98` suites / `663` tests, model `5` / `51`, plugin `1` / `18`, all ABI/supply-chain/release-candidate checks, and docs `45` / `61`. The current AndroidX contract has five required methods: the historical codec/SQLite three plus 32 accepted emits and one HIGH admission into a full 2,048-event queue. No physical device was connected, so the two new Dispatcher budgets are compiled, fail-closed release requirements rather than accepted physical measurements; historical three-method JSON cannot pass the current gate.

Fresh checks executed on `2026-07-16` against the completed client-closure tip:

```powershell
./gradlew.bat testDebugUnitTest --rerun-tasks --no-daemon
./gradlew.bat :apm-model:test --rerun-tasks --no-daemon
./gradlew.bat assembleDebug --no-daemon
./gradlew.bat -p apm-plugin test --rerun-tasks --no-daemon
./gradlew.bat :apm-benchmark:assembleRelease :apm-benchmark:compileReleaseAndroidTestKotlin --no-daemon
./gradlew.bat lintDebug assembleRelease publishToMavenLocal --no-daemon
./gradlew.bat -p smoke-tests/maven-consumer clean assembleDebug --no-daemon
```

All commands passed under JDK `17.0.14`. Root Gradle XML reports contain `94` suites and `595` tests with `0` failures/errors/skips; the included `apm-plugin` build separately passed `18` tests. Lint produced `23` HTML reports; the sample Release artifact is `apm-sample-app-release-unsigned.apk` (`4,708,872` bytes). Maven Local contains the current `com.apm:*-0.1.0` publications (`21` AAR, `23` JAR, `22` POM); `apm-benchmark` is absent from Maven Local, and the isolated consumer resolved the published SDK modules successfully. Representative model, Android library, Gradle plugin, and sample classes all report class-file major version `61` (Java 17). The root build configuration and `:apm-model:test` also passed with Gradle launched by JDK `21.0.11`; the resulting model class remained major version `61`. A Xiaomi `22041216UC` and Android 17 emulator were visible to ADB: physical benchmark installation was blocked by device policy (`INSTALL_FAILED_USER_RESTRICTED`), while the emulator completed all three benchmark methods and generated JSON/Perfetto output after suppressing the expected `EMULATOR` gate, but the task ended on an AndroidX `IsolationActivity` launch timeout. These emulator values are execution evidence, not physical performance claims; accepted physical measurements remain external validation.

Incremental P0 hardening checks on `2026-07-21` used JDK `17.0.14`: `:apm-core:testDebugUnitTest --no-daemon` passed `23` suites / `153` tests with zero failures, and `python docs/verify_docs.py` passed `40` Markdown files / `37` local links. A root `testDebugUnitTest` attempt that day was invalidated by external Gradle-cache deletion; it was an environment failure, not a passing or failing source baseline. The stable-cache full refresh on `2026-07-22` below supersedes the older `595`-test baseline.

Second-batch hardening checks on `2026-07-22` used JDK `17.0.14`: `:apm-core:testDebugUnitTest :apm-storage:testDebugUnitTest --no-daemon` passed `23` suites / `160` tests and `6` suites / `36` tests respectively, with zero failures/errors/skips. The storage suite was rerun with `--rerun-tasks` after the chunked trim hardening and passed all `36` tests. `python docs/verify_docs.py` again passed `40` Markdown files / `37` local links. This was a focused incremental result; the current full-root baseline is the post-closure refresh below.

Third-batch measurement/self-observability checks on `2026-07-22` used JDK `17.0.14`: `:apm-fps:testDebugUnitTest :apm-core:testDebugUnitTest --rerun-tasks --no-daemon` passed `4` suites / `31` tests and `23` suites / `162` tests respectively, with zero failures/errors/skips. After the API 26 delayed-frame guard was added, `:apm-fps:testDebugUnitTest :apm-fps:lintDebug --no-daemon` reran successfully. Current `apm-fps` and `apm-core` lint reports both say `No issues found`, and `python docs/verify_docs.py` passed `40` Markdown files / `37` local links. FPS reporting now uses a one-second monotonic time window by default, and FrameMetrics aggregation has deterministic rolling-window tests without per-frame report-object allocation. `sdk_health` has an independent diagnostics summary plus a high-priority telemetry attempt. This was a focused result; the current full-root baseline is below.

Fourth-batch dispatcher isolation checks on `2026-07-22` used JDK `17.0.14`: `:apm-core:testDebugUnitTest :apm-core:lintDebug --rerun-tasks --no-daemon` passed `23` suites / `167` tests with zero failures/errors/skips, and the lint report says `No issues found`. `python docs/verify_docs.py` passed `40` Markdown files / `37` local links. Deterministic queue-pressure tests cover default module isolation, peer-capacity preservation, HIGH-priority bypass/eviction, percentage clamping, the disable switch, aggregate drop accounting, and the dedicated `dispatcherModuleIsolationDropCount`. This was focused isolation evidence; the current full-root baseline is below.

Fifth-batch business-context latency checks on `2026-07-22` used JDK `17.0.14`: `:apm-core:testDebugUnitTest :apm-core:lintDebug --rerun-tasks --no-daemon` passed `24` suites / `172` tests with zero failures/errors/skips, and the lint report says `No issues found`. `python docs/verify_docs.py` passed `40` Markdown files / `37` local links. Tests cover synchronous immutable snapshots, async SDK-thread execution, initial-empty/LKG behavior, recoverable provider failure, explicit-request coalescing, and public init/refresh/stop lifecycle. This was focused latency evidence; the current full-root baseline is below.

Sixth-batch consumer-distribution checks on `2026-07-22` used JDK `17.0.14`: the root `publishToMavenLocal --no-daemon` task passed, the isolated Maven consumer passed `clean assembleDebug --no-daemon` with only `com.apm:apm-bundle:0.1.0`, and focused `:apm-bundle:lintDebug :apm-bundle:assembleRelease --no-daemon` passed. Maven Local contains `22` AAR, `24` JAR, and `23` POM files for `com.apm:*-0.1.0`; the bundle POM exposes all `22` runtime SDK artifacts, while the bundle AAR carries no SDK implementation classes. Documentation verification passed `41` Markdown files / `39` local links. This focused result validates local publication and transitive consumer compilation; Maven Central or another external repository remains unverified.

Seventh-batch HttpURLConnection integration checks on `2026-07-22` used JDK `17.0.14`: `:apm-network:testDebugUnitTest :apm-network:lintDebug --rerun-tasks --no-daemon` passed `3` suites / `21` tests with zero failures/errors/skips, and the lint report says `No issues found`. Tests cover successful and HTTP-error responses, header/body transport failures, non-transport host exceptions, stopped-module execution, recoverable report failure isolation, and fatal VM error visibility. Documentation verification passed `41` Markdown files / `39` local links. This focused result validates the explicit public-API wrapper, not real proxy/TLS/OEM network behavior.

Eighth-batch performance-budget checks on `2026-07-22` used JDK `17.0.14`: the five host verifier tests passed, existing emulator JSON exercised all three configured budget comparisons only with the explicit parser-only override, and `:apm-benchmark:assembleRelease :apm-benchmark:compileReleaseAndroidTestKotlin --rerun-tasks --no-daemon` passed. The checked-in gate covers median time and allocations for durable encode/decode plus the 32-event SQLite transaction, fails on missing/malformed/over-budget evidence, and rejects emulator results by default. Documentation verification passed `42` Markdown files / `41` local links. An accepted physical-device gate run remains external validation because the visible Xiaomi device still blocks test-APK installation.

Ninth-batch typed-durable-field checks on `2026-07-22` used JDK `17.0.14`: `:apm-model:test :apm-storage:testDebugUnitTest :apm-benchmark:compileReleaseAndroidTestKotlin --rerun-tasks --no-daemon` passed model `4` suites / `40` tests and storage `6` suites / `36` tests with zero failures/errors/skips. Durable codec v3 preserves null, String, Boolean, Byte, Short, Int, Long, Float, Double, Char, BigInteger, and BigDecimal values; legacy v1/v2 payloads retain string semantics, unsupported objects fall back to bounded strings, arbitrary-precision parsing is capped at 4,096 characters, and unknown type tags reject only the corrupt event. Documentation verification passed `42` Markdown files / `41` local links. At that milestone, legacy Line Protocol and standalone Protobuf fields remained string-valued, so the durable change did not silently alter their collector contract; the later V2 envelope below is an independent opt-in schema.

Post-closure full-root checks on `2026-07-22` used JDK `17.0.14`: root `testDebugUnitTest --rerun-tasks --no-daemon` passed `92` suites / `605` tests, the same-source model run passed `4` suites / `40` tests, and `apm-plugin test --rerun-tasks --no-daemon` passed `1` suite / `18` tests, all with zero failures/errors/skips. Root Android plus model therefore establishes a current `96`-suite / `645`-test client baseline; the included plugin remains reported separately. Documentation verification passed `42` Markdown files / `41` local links. This supersedes the `2026-07-16` `595`-test root baseline.

Tenth-batch strict-production/consent checks on `2026-07-22` used JDK `17.0.14`: `:apm-core:testDebugUnitTest --rerun-tasks --no-daemon` passed `25` suites / `180` tests with zero failures/errors/skips, `:apm-core:lintDebug --rerun-tasks --no-daemon` passed with `No issues found`, and `python docs/verify_docs.py` passed `42` Markdown files / `41` local links. Tests cover strict/compatibility validation, explicit consent, active-runtime outbox erase, cold-start dormant erase, sticky re-init denial, and IPC artifact cleanup. This is focused current-source evidence; the preceding `605`-test root result remains the most recent full-root run rather than being silently extrapolated to this change.

Eleventh-batch collector-wire-V2 checks on `2026-07-22` used JDK `17.0.14`: `:apm-model:test :apm-uploader:testDebugUnitTest :apm-uploader:lintDebug :apm-core:testDebugUnitTest :apm-core:lintDebug --rerun-tasks --no-daemon` passed model `5` suites / `46` tests, uploader `4` suites / `24` tests, and core `25` suites / `180` tests with zero failures/errors/skips; both lint reports say `No issues found`. `python docs/verify_docs.py` passed `43` Markdown files / `47` local links. Tests cover all supported typed scalars, append-only event field 15, stable batch identity, fixed resource metadata, encoded-size splitting/rejection, protocol-owned request headers, exact whole-batch ACK, strict-profile protocol/resource validation, and legacy compatibility. This is focused current-source evidence; the preceding `605`-test root result remains the most recent full-root run.

Twelfth-batch critical-handoff/loss-attribution checks on `2026-07-22` used JDK `17.0.14`: `:apm-core:testDebugUnitTest :apm-core:lintDebug :apm-storage:testDebugUnitTest :apm-storage:lintDebug :apm-anr:testDebugUnitTest :apm-anr:lintDebug --rerun-tasks --no-daemon` passed core `27` suites / `184` tests, storage `6` suites / `37` tests, and ANR `5` suites / `26` tests with zero failures/errors/skips; all three lint reports say `No issues found`. Documentation verification passed `43` Markdown files / `47` local links. Tests cover critical-priority promotion, synchronous ANR hand-off success/failure, remote-process IPC rejection attribution, queue/storage/uploader drop reasons, reason/priority/reset invariants, explicit unattributed totals, SQLite capacity/prune priority recovery, and fatal-error visibility. This is focused current-source evidence; the preceding `605`-test root result remains the most recent full-root run.

Thirteenth-batch time-semantics/event-snapshot checks on `2026-07-22` used JDK `17.0.14`: focused unit tests across core plus 15 monitoring/extension modules passed `77` suites / `527` tests with zero failures/errors/skips, and root `lintDebug --rerun-tasks --no-daemon` passed. A same-source full refresh then passed root `96` suites / `630` tests, model `5` suites / `46` tests, and included plugin `1` suite / `18` tests, all with zero failures/errors/skips; root Android plus model is now `101` suites / `676` tests, with plugin reported separately. `python docs/verify_docs.py` passed `43` Markdown files / `47` local links. Tests cover monotonic duration/expiry/dedup/rate-limit windows, epoch collector timestamps, end-before-start span handling, rendered-FPS interval semantics, and immutable direct-event maps at asynchronous hand-off boundaries. This supersedes the preceding `605`-test root result. Wall-clock values remain only where the wire/persistence/file/HTTP contract requires an epoch; in-process elapsed measurements use `ApmClock` monotonic time.

Fourteenth-batch cross-layer byte-budget checks on `2026-07-22` used JDK `17.0.14`: `:apm-core:testDebugUnitTest :apm-core:lintDebug --rerun-tasks --no-daemon` passed `27` suites / `194` tests with zero failures/errors/skips and `No issues found`; `:apm-storage:testDebugUnitTest :apm-storage:lintDebug --rerun-tasks --no-daemon` passed `6` suites / `37` tests with zero failures/errors/skips and `No issues found`. Tests cover conservative caller-side event sizing, dispatcher count-plus-byte admission, multi-victim lower-priority eviction, exact byte-budget drop attribution, lock-free IPC pending work with one fixed-delay writer task, per-event/file/directory IPC budgets, synchronous critical rejection reasons, streaming ready-file reads, and the existing SQLite per-event/live-payload byte limits. A same-source full refresh passed root `96` suites / `636` tests, model `5` suites / `46` tests, and included plugin `1` suite / `18` tests, all with zero failures/errors/skips; root Android plus model is now `101` suites / `682` tests, with plugin reported separately. `python docs/verify_docs.py` passed `43` Markdown files / `47` local links. This supersedes the Thirteenth-batch `630`-test root baseline.

Fifteenth-batch physical-device-soak gate checks on `2026-07-22` used JDK `17.0.14`: all `14` host Python tests passed, and `:apm-sample-app:assembleDebug :apm-sample-app:lintDebug :apm-benchmark:assembleRelease :apm-benchmark:compileReleaseAndroidTestKotlin --no-daemon` passed; sample lint reports `0` errors / `25` warnings. The sample now supports a zero-SDK A/B control, an always-failing offline uploader, bounded main-thread synthetic operations, and app-private per-process results. The host runner samples cold start, SDK init, `/proc` CPU, PSS, app-private disk, app-UID batterystats power, charge counter, and thermal state across process restarts. Fail-closed `smoke`, `24h`, and `72h` profiles require a physical device, actual elapsed duration, restart count, offline mode, complete resource fields, and long-profile power evidence. The visible Xiaomi `22041216UC` rejected the freshly built `5,916,050`-byte debug sample APK with `INSTALL_FAILED_USER_RESTRICTED: Install canceled by user`, so no physical values or 24/72-hour acceptance are claimed. `python docs/verify_docs.py` passed `43` Markdown files / `48` local links. This focused result does not replace the Fourteenth-batch `636`-test full-root baseline.

Physical-device execution checks on `2026-07-23` used the same JDK `17.0.14` source state and a physical Redmi/Xiaomi `22041216UC` on Android 13. `:apm-sample-app:assembleDebug :apm-benchmark:assembleRelease :apm-benchmark:compileReleaseAndroidTestKotlin --no-daemon` passed, direct ADB installation succeeded after the device USB-install controls were enabled, and the official AndroidX `AndroidBenchmarkRunner` completed `3/3` tests after the test package alone was temporarily allowed to open its isolation activity in the background; that MIUI app-op was restored to its original `ignore` mode after the run. The fail-closed JSON verifier accepted durable encode at `4,640.93 ns / 22.00 allocations`, decode at `4,841.81 ns / 46.00 allocations`, and the 32-event SQLite transaction at `1,258,990.52 ns / 1,400.21 allocations`. The Gradle aggregate task itself still cannot be called clean on this OEM because its session-based test-APK installation is rejected with `INSTALL_FAILED_USER_RESTRICTED`; the accepted metrics came from directly installing the exact built test APK and running its declared benchmark runner without suppressing AndroidX errors. Two complete physical `smoke` acquisitions both failed only the checked-in `20%` average-CPU ceiling: `28.425%` and `32.046%`; duration, two process starts, offline mode, startup delta, SDK init, main-thread P95, PSS growth, disk growth, and thermal status passed. No `24h` or `72h` run or long-profile power acceptance is claimed.

CPU attribution and repair checks later on `2026-07-23` used the same physical device and kept the checked-in `20%` smoke ceiling unchanged. A steady-state per-thread `/proc` sample attributed about `26.4%` CPU to the enabled process main thread while the SDK-disabled control main thread used about `0.8%`; the measured `Apm.emit` P95 at 10 events/second could not explain that gap. The root cause was the FPS monitor perpetually reposting a Choreographer callback on an otherwise static screen. API 24+ now uses event-driven `FrameMetrics` as the primary real-render source and schedules Choreographer only when FrameMetrics is disabled or registration fails. JDK `17.0.14` `:apm-fps:testDebugUnitTest :apm-fps:lintDebug :apm-sample-app:assembleDebug --rerun-tasks --no-daemon` passed; the final FPS refresh reports `4` suites / `34` tests with zero failures/errors/skips and lint `No issues found`. Two fresh physical smoke runs of the identical APK SHA-256 `e22185f6b09182e5705cea27d80f74f3ac4f05d89ac2223638c02bc4e8f55c1d` passed all original budgets with CPU `12.928%` and `12.362%`. The runs observed `30.562s` / `30.534s`, two process starts each, main-thread P95 `1,853.462us` / `1,796.539us`, and no budget override. `24h`/`72h` and long-profile power evidence remain unexecuted.

The first-principles review was source-audited again on `2026-07-23` and now separates client architecture, short physical evidence, long/OEM validation, and end-to-end product readiness instead of assigning an unsupported single numeric score. It also records that `PRODUCTION_STRICT` is opt-in, Collector deduplication remains external, and device-soak `cpuAveragePercent` is the enabled process's absolute CPU rather than a control-subtracted delta. `python docs/verify_docs.py` passed `43` Markdown files / `49` local links after this correction.

Device-soak CPU attribution was then made explicit without changing any checked-in ceiling. Result schema version `2` retains `cpuAveragePercent` as the enabled process's absolute gate, adds raw-sample-derived `cpuControlPercent`, `cpuEnabledAveragePercent`, and signed `cpuDeltaPercent`, and requires the verifier to recompute and reconcile those values from control/enabled jiffies. Result schema version `1` remains readable under its original absolute CPU semantics. All `17` host Python tests passed, and an accepted historical schema-v1 physical smoke artifact still passed the unchanged `20%` gate. This host-only update does not create new schema-v2 physical evidence or replace the pending `24h`/`72h` runs.

Uploader thread-governance checks on `2026-07-23` used JDK `17.0.14`: `:apm-uploader:testDebugUnitTest :apm-uploader:lintDebug --rerun-tasks --no-daemon` passed `4` suites / `25` tests with zero failures/errors/skips, and lint reports `No issues found`. `RetryingApmUploader` preserves the dependency boundary that prevents `apm-uploader` from depending on `apm-core`, but both its ordered worker and delayed-retry scheduler now come from one module-local named factory that explicitly sets daemon status and `Thread.MIN_PRIORITY`. The regression test observes both actual delegate-execution threads and verifies their names, daemon status, and priority. `python docs/verify_docs.py` passed `43` Markdown files / `49` local links, and both generated DOCX reports contain valid package/document entries plus the updated thread-governance statement.

Dispatcher stage-latency evidence checks on `2026-07-23` used JDK `17.0.14`: `:apm-core:testDebugUnitTest :apm-core:lintDebug --rerun-tasks --no-daemon` passed `27` suites / `197` tests with zero failures/errors/skips, and lint reports `No issues found`. When self-monitoring is enabled, the existing single worker now measures fixed `resolve`, `sampling`, `aggregate`, `rateLimit`, `sanitize`, and `storeHandoff` stages with monotonic nanoseconds and reports count, ceiling average microseconds, conservative P95 histogram-bucket upper bound, and maximum microseconds. Recording uses six fixed 22-bucket histograms, no per-sample allocation, and a short coherent snapshot lock; when self-monitoring is disabled, the dispatcher bypasses clock reads and recording entirely. These fields are evidence only and do not automatically throttle, repartition, or parallelize the dispatcher. No physical or long-duration stage-latency distribution is claimed by the focused unit/lint result. `python docs/verify_docs.py` passed `43` Markdown files / `49` local links, and both generated DOCX reports contain valid package/document entries plus the updated P95 evidence statement.

OnePlus Android-16 device-soak compatibility checks on `2026-07-23` covered the physical `PLK110` OEM policy that denies ADB shell `pm clear` with `SecurityException` / `android.permission.CLEAR_APP_USER_DATA`. The runner now falls back only for that exact denial by uninstalling and reinstalling the selected sample APK; other ADB failures remain fail-closed, and the artifact records `appDataResetStrategy`. All `21` host Python tests passed, and JDK `17.0.14` `:apm-benchmark:assembleRelease :apm-benchmark:compileReleaseAndroidTestKotlin --no-daemon` completed successfully. A schema-v2 physical smoke of APK SHA-256 `213f5d73c93472b77da0909ac7be4c4cb92ededcc00a3dc5bfa548681689cb59` then passed unchanged budgets with `30.225s`, two starts, enabled CPU `6.161%`, control CPU `6.793%`, main-thread P95 `354.896us`, and app-UID power `29.062 mAh/hour`; reset provenance is `uninstall-reinstall`. `python docs/verify_docs.py` passed `43` Markdown files / `49` local links, and both DOCX reports were regenerated and validated for the OnePlus/power evidence. This proves reset and power-acquisition readiness on a second OEM/API level, not `24h` or `72h` acceptance.

Bounded ADB transport-recovery checks later on `2026-07-23` passed all `25` host Python tests. Only explicitly read-only evidence commands retry `device offline`, exact serial-not-found, or no-device errors, for at most `30` attempts at one-second intervals; install, uninstall, `pm clear`, Activity start, and force-stop are never replayed. Exhaustion remains fail-closed even for optional smoke power evidence. Artifacts record `transientAdbRetryCount`, and the verifier validates this non-negative integer plus the known reset strategy. A fresh OnePlus smoke then passed unchanged budgets with retry count `0`, `30.221s`, two starts, enabled CPU `5.134%`, control CPU `7.164%`, main-thread P95 `360.677us`, PSS growth `3,327 KiB`, disk growth `49,152 bytes`, and app-UID power `29.423 mAh/hour`. This validates normal-path command classification before the long campaign, not `24h` completion.

The first background OnePlus `24h` attempt from commit `97cdc90` ended during its first hour after the device remained absent beyond the then-common 30-second read-only reconnect window. The retained stderr identifies `shell pidof ... device not found`; no result JSON exists, so no duration, restart, power, or acceptance claim is made. The reconnect policy is now profile-aware: smoke remains 30 seconds, `24h`/`72h` default to 300 seconds, and CLI overrides are capped at 600 seconds. Artifacts/verifier bind both the window and retry count while mutating commands remain single-attempt. All `26` host Python tests and Python bytecode compilation passed. The Redmi `22041216UC` then returned and passed a current-code smoke with `30.553s`, two starts, CPU `11.319%`, main-thread P95 `1,838.231us`, window `30s`, retry count `0`, and `pm-clear`; the result hash is `e2893814fc981ece690e9452e14f324e7933f9b186c0e7003b44874424ea4c15`. It still produced no UID power evidence, so no knowingly incomplete long profile was started at that milestone; the next paragraph supersedes the then-current device choice.

Redmi/MIUI UID-power hardening on `2026-07-24` used the currently available physical `22041216UC`. Android's package-scoped human-readable `batterystats` omitted UID `10217`, while the official current checkin form retained `9,10217,l,pwi,uid,...`; the runner now uses that exact UID Power Use Item only as a fallback. A cumulative UID value must increase strictly across the enabled phase before it becomes `android-batterystats-uid` evidence: a flat zero, reset, regression, malformed/non-finite value, wrong UID, or missing item remains absent instead of being clamped to a passing `0 mAh/hour`. All `30` host Python tests and Python bytecode compilation passed, and JDK `17.0.14` `:apm-benchmark:assembleRelease :apm-benchmark:compileReleaseAndroidTestKotlin --no-daemon` passed. A five-minute current-device diagnostic at the unchanged `10` events/second left the MIUI checkin value at `0`, so it is not power acceptance. No checked-in budget changed.

On `2026-07-25`, the project decision changed: keeping a personal phone attached for a `24h` run is not required for the current client-SDK iteration. The active Redmi retry was explicitly cancelled before completion, the host runner and sample process were stopped, and no result JSON exists; this is neither a passing nor a failing long-profile artifact. The fail-closed `24h`/`72h` profiles remain implemented and unchanged, but their execution is deferred to pre-production validation on managed device-lab hardware or calibrated power infrastructure. Current claims remain limited to the accepted physical microbenchmarks and original-budget smoke runs.

Release-candidate and supply-chain checks on `2026-07-31` use strict SHA-256 Gradle dependency-verification metadata in the root build, `apm-plugin`, `build-logic`, and the isolated consumer; the policy verifier covers `554` / `238` / `239` / `409` dependency components respectively and limits consumer trust to generated `com.apm` artifacts that the release verifier hashes directly. The isolated candidate contains `25` coordinates, `22` AAR, `26` JAR, and `25` POM files: the `23` root publications plus `com.apm:apm-plugin` and the `com.apm.slow-method` marker. The verifier checks exact POM and dependency graphs, sources and Gradle metadata, fixed versions, Java-17 class major, ZIP traversal/secret-like names, signature completeness, and emits a per-file SHA-256 manifest plus an SPDX 2.3 distribution SBOM. Five host tests cover valid, missing, dependency-tampered, ZIP-traversal, and signature-policy branches; the isolated consumer resolved both Bundle and plugin from only the candidate repository and completed ASM transformation plus `assembleDebug`. Generic external publishing is created only for an HTTPS repository and fails before upload when credentials or PGP configuration are missing. No real Maven Central/private staging, promotion, or external download evidence is claimed.

Managed-device-lab asset checks on `2026-07-31` used JDK `17.0.14`: all `40` benchmark/device-lab host Python tests and Python bytecode compilation passed; the checked-in plan validated `3` non-overlapping physical ARM64 API lanes and `6` lane/profile requirements while explicitly evaluating no physical evidence. `:apm-benchmark:verifyDeviceLabMatrix :apm-benchmark:assembleRelease :apm-benchmark:compileReleaseAndroidTestKotlin :apm-sample-app:assembleDebug --rerun-tasks --no-daemon` passed `504` executed Gradle actions. The complete `python tools/verify_ci.py` gate also passed the `24` ABI baselines, Android `98` suites / `654` tests, model `5` suites / `46` tests, plugin `1` suite / `18` tests, all with zero failures/errors/skips, plus the matrix plan, `40` host tests, and docs. Result schema `3` binds exact APK, clean source revision, matrix/budget/runner hashes, lane, timestamp, and device identity; the aggregate gate requires one APK/source plus lane/profile, device/manufacturer, and reset-strategy coverage. Documentation verification passed `44` Markdown files / `55` local links. Both DOCX reports were regenerated and passed package/relationship/XML/key-text checks; the workstation still lacks LibreOffice/`soffice`, so no page-render visual acceptance is claimed. No schema-v3 `24h` or `72h` physical result exists.

## Project Boundary

AndroidAPM is a modular Android client SDK, not a complete hosted APM product. It captures, normalizes, protects, persists, and transports telemetry. A production collector, authentication, tenant isolation, query/aggregation backend, alerting, native symbolization service, and operational dashboards are outside this repository.

The default runtime path is:

```text
monitor module
  -> Apm.emit (caller captures timestamp/thread/context/payload snapshots)
  -> bounded dispatcher queue (2048 events + 8 MiB estimated retained bytes; default 75% high-water / 50% per-module NORMAL/LOW share; higher priority may evict enough oldest lower-priority entries; producers never wait)
  -> signed dynamic sampling, optional aggregation, dynamic rate limiting, and default-on PII sanitization
  -> appendBatch (up to 32 drained events)
  -> SQLite durable outbox v4 (50,000 rows; 64 MiB live-payload budget; 256 KiB per-event soft limit; unique eventId; `idx_priority_desc_ts` claim ordering index added at schema v4, v3→v4 is additive)
  -> claim(owner, lease, expiry) -> PersistentUploadWorker
  -> BatchApmUploader / HttpApmUploader / custom uploader
  -> integrator-owned collector
```

Crash and ANR use the dedicated `Apm.emitCriticalSync` path: caller-supplied lower priority is promoted to CRITICAL, the shared queue/sampling/aggregation/rate-limit path is bypassed, and success means the complete event reached SQLite or a synchronously published critical IPC file. The path never performs blocking network IO. The uploader process sends CRITICAL IPC events directly to the synchronous store boundary and deletes the ready file only after acceptance; false, recoverable failure, or no consumer retains the complete file for at-least-once retry. Published `.ipc` files do not age out before acceptance; the directory byte budget bounds them, while incomplete `.tmp` files retain the five-minute cleanup. Optional normal multi-process forwarding bounds pending events to 4 MiB, raw encoded events to 256 KiB, each ready file to 1 MiB, and the ready directory to 16 MiB; producers use a lock-free pending queue, one fixed-delay writer task performs batching, and complete `.tmp` files publish as `.ipc` files under a cross-process budget lock.

Delivery is acknowledged and at least once. Stable `eventId` survives Line Protocol, Protobuf field 14, durable codec, SQLite, and IPC. SQLite write transactions atomically claim rows with owner/expiry; only the owner may acknowledge or fail them, shutdown releases claims, and expired claims are reclaimable. Ambiguous network completion can still retransmit, so the collector must deduplicate by `eventId`.

Durable codec format v3 adds explicit scalar type tags for event `fields` while continuing to read v1/v2 rows. Supported scalar types round-trip exactly; arbitrary objects deliberately degrade through `toString()` rather than Java serialization. Legacy Line Protocol and standalone Protobuf remain string-valued. `PROTOBUF_ENVELOPE_V2` is a separate explicit collector contract with field-15 typed values, fixed resource context, schema/SDK versions, retry-stable batch IDs, a 1 MiB default/4 MiB absolute uncompressed request budget, and exact whole-batch ACK headers; it does not reinterpret legacy payloads.

## Integration Reality

Registration alone does not make every monitor automatic:

- Network requires the OkHttp interceptor/listener, explicit `traceHttpUrlConnection`, or manual completion callbacks. The HttpURLConnection helper executes by reading `responseCode`, does not consume/disconnect the host connection, reports only total duration/status/header-derived size, and is not a global hook.
- SQLite requires `ApmSQLiteDatabase` or `onSqlExecuted` callbacks. Only wrapper `rawQuery` has the database handle, full SQL, and bound arguments needed for threshold-gated `EXPLAIN QUERY PLAN`; monitoring reports are isolated from host database results/exceptions.
- IPC uses `traceBinderCall` or `onBinderCallComplete`; deprecated `enableBinderHook=false` does not use hidden APIs.
- WebView uses explicit per-instance `install/uninstall`, delegate wrappers, `evaluateJavascript`, or callbacks; deprecated global `enableAutoRegister=false` does not take over arbitrary instances.
- Battery WakeLock/GPS/Alarm signals are host callbacks.
- IO uses explicit stream wrappers and an optional xhook-backed native path. Deprecated `enableAutoHook=false` does not take over arbitrary Java streams; Java ThreadLocal call-path suppression prevents double counting without dropping custom streams, Native callback-depth prevents APM-owned IO recursion, fd sessions use mutex/generation protection, duplicate/small-buffer findings report once per bounded path, and wrapper bookkeeping cannot fail completed host IO.
- Slow-method ASM requires the host module to apply `com.apm.slow-method`.
- FPS reports on a monotonic interval (`reportIntervalMs=1000` by default) rather than after a fixed frame count. Rendered FPS is measured actual rendered-frame callback intervals divided by their elapsed nanoseconds, capped by the display refresh rate, and reports `windowDurationMs`; the deprecated `windowSize` remains source-compatible but no longer controls cadence. API 24+ uses event-driven FrameMetrics as the primary source with a bounded primitive rolling accumulator; perpetual Choreographer callbacks are only the disabled/failed-FrameMetrics fallback so a static screen is not kept awake by the observer.
- Render measures view count/depth and API 24+ FrameMetrics. Deprecated `detectOverdraw=false` is truthful because no supported GPU overdraw counter exists.
- Thread monitoring inspects count/name/BLOCKED state; real ThreadPoolExecutor backlog requires explicit registration. Generic leak fields are deprecated/false.
- GC count/time/allocation/reclaim analysis consumes monotonic ART cumulative counters on an `ApmExecutors` background sampler; missing/reset counters invalidate only that dimension/window, invalid intervals clamp to one second, and available heap dimensions continue.
- Business context defaults to occurrence-time synchronous capture for compatibility; such providers must be O(1), perform no IO, and wait on no contended locks. `ASYNC_CACHED` runs the provider only on `apm-biz-context`, keeps the last good immutable snapshot after recoverable failure, bounds refresh cadence to 100 ms–24 h, and lets `Apm.refreshBizContext()` request one coalesced background refresh after login or tenant changes. Async emit reads never call host provider code.
- Reliability priority is host safety, then telemetry durability, then diagnostic completeness. Recoverable `Exception` failures are isolated at dispatcher/store/uploader/diagnostics boundaries; fatal VM errors are not converted into drops or retries. The shared dispatcher remains a single worker, but it now enforces both the 2,048-event and default 8 MiB estimated-retained-byte budgets. Default-on high-water isolation stops one NORMAL/LOW module from consuming more than 50% of total queue capacity after 75% occupancy; HIGH/CRITICAL bypass that gate and may evict enough older lower-priority entries to satisfy both dimensions. Multi-process coordination separately bounds pending, encoded event, ready-file, and ready-directory bytes and never creates one executor task per event. SQLite isolates an invalid/oversized event from valid batch peers, caps one durable payload at 256 KiB by default, and trims low-priority old rows against a 64 MiB live-payload budget as well as the 50,000-row budget; storage rejection/eviction increments SDK drop health. Active leases remain protected and physical SQLite page/WAL size is not the live-payload metric. Retry hints are bounded to 60 seconds; `maxRetries` means retries after the initial attempt and exhausted rows are pruned immediately after `maxRetries + 1` failures. Cached outbox deletion counts cannot become negative, and diagnostics export failures return failure data.
- `apm-otel-exporter` maps data only; it does not depend on or send through the OTel SDK.
- `apm-remote-config` polls authenticated HTTPS with ETag, verifies canonical JSON with pinned Ed25519/Tink keys, durably records the highest revision and LKG, and publishes only non-expired verified values. It drives global/module kill switches and event sampling/rate limits; endpoint rotation is separately opt-in and accepts only HTTPS.
- `apm-bundle` is a transitives-only convenience artifact that exposes all published client modules from one dependency. It does not initialize/register modules or apply `com.apm.slow-method`; size-sensitive consumers should keep selecting fine-grained artifacts.
- `apm-benchmark` has two independent measurement gates plus a managed-device aggregate contract. AndroidX Microbenchmark protects codec/SQLite/Dispatcher median time and allocations; the historical physical result covers only the original three methods, while the current five-method contract still needs managed-device execution. `run_device_soak.py` drives the debug sample through a zero-SDK control and always-failing-uploader enabled phases, then checks cold start, SDK init, main-thread operation P95, process CPU/PSS, app-private disk, app-UID power, thermal status, actual elapsed duration, and restarts for `smoke`/`24h`/`72h`. The required `--reset-app-data` clears only the selected sample package before acquisition. Schema v3 binds exact APK/source/matrix/budget/runner/lane/device provenance, while the aggregate verifier requires all three API lanes, six lane/profile artifacts, OEM/device/reset diversity, and one APK/source. Plan validation and host tests/builds never count as accepted physical results.

Important defaults: `ApmRuntimeProfile.COMPATIBILITY` preserves the existing empty/unsupported endpoint safe-discard behavior and explicit `logcat://` delivery. `PRODUCTION_STRICT` fails before diagnostics/storage/thread creation unless consent is explicitly `GRANTED`, sanitization is enabled, debug logging is disabled, storage is SQLite, and delivery uses an exact HTTPS endpoint or an explicit non-Logcat custom uploader. The built-in strict HTTP path additionally requires `PROTOBUF_ENVELOPE_V2`, complete bounded standard resource identity, and enough batch-byte headroom above the durable event limit. `Apm.revokeCollectionConsent(application)` is the cold-start-safe erase API: it makes revocation sticky, stops an active runtime without draining/aggregation flush, stops delivery before erasing, and clears SQLite, File, and IPC event artifacts; every SDK process must invoke revocation to close its in-memory producers. Dynamic endpoint rotation, aggregation, multi-process coordination, native crash, Hprof dump, and fork dump remain opt-in. Static HTTP headers and the per-request credential provider are empty.

AutoThrottle degradation is immediate and sticky. Recovery requires three consecutive periods at or below 20% drop rate and 3 seconds average upload latency; a degraded or hysteresis-band period resets the recovery streak. Registration and signed dynamic-config reconciliation cannot restart a module while it is held by auto-throttle, and recovery still rechecks process/dynamic/gray gates.

SDK self-diagnostics are separate from event delivery. They are enabled by default and bound both the 200-record memory ring and 256-record writer queue to 4 MiB each, plus up to three 512 KiB app-private JSONL segments per Android process. Process journals are isolated; aggregate export selects up to 16 recent process directories and is bounded to 10,000 records / 16 MiB of uncompressed JSONL. Each real loss increments the aggregate drop count plus a stable reason and LOW/NORMAL/HIGH/CRITICAL priority counter; aggregate-only compatibility results use an explicit `UNATTRIBUTED` priority rather than inventing one. Capacity eviction and retry/age pruning carry exact SQLite priority counts. Each periodic `sdk_health` report first records this payload-free numeric summary, including dispatcher module-isolation drops, current queued estimated bytes, and bounded fixed-stage latency count/average/P95-upper-bound/max fields, in the independent journal, then attempts normal telemetry at HIGH priority; dispatcher and IPC byte-budget rejections use stable reasons rather than collapsing into generic queue/file failures. The journal copy does not depend on dispatcher/outbox/uploader. `ApmDiagnostics` exposes cached status, synchronous/async snapshot and ZIP export, current-process clear, explicit all-process clear, and a file-free `hostIntegrationSnapshot()` for Network/SQLite/IPC/WebView/thread-pool/Battery/IO runtime wiring evidence. Host integration evidence stores only fixed enums, counters, and timestamps; `NO_RUNTIME_EVIDENCE` means the operation has not occurred in the current init/stop session, not that static integration is proven broken. Diagnostics are not automatically uploaded.

## Working Rules

- Add KDoc for all `public`, `internal`, and `private` properties and methods.
- Add inline comments at important branches, loops, exception handling, callbacks, and business-significant assignments.
- Extract non-trivial magic numbers and strings into named constants.
- Create SDK threads/executors through `com.apm.core.ApmExecutors`; `apm-uploader` retains module-local executors because it cannot depend on `apm-core`, and those executors must use its explicit named daemon/background thread factory.
- Report degraded-and-swallowed exceptions through `Apm.recordInternalError(tag, error)`.
- Do not route diagnostics file-sink failures back through `ApmLogger` or `Apm.recordInternalError`; that path must remain non-recursive.
- Preserve the durable SQLite outbox as the default storage path.
- Preserve SQLite transaction-scoped claim selection, owner-aware ACK/failure, expiry reclaim, and active-lease prune/trim protection.
- Preserve fail-closed strict-profile validation before SDK resource creation and consent-revocation ordering: reject new events, stop delivery, then erase queued/persisted telemetry without a graceful flush.
- Do not claim a config switch is an automatic hook unless a source-backed runtime path consumes it.

## Git and Documentation Policy

- Commit messages are English `Type: Subject`.
- Allowed prefixes: `Feat`, `Fix`, `Refactor`, `Perf`, `Style`, `Docs`, `Revert`, `Build`.
- `docs/` is tracked and is part of the deliverable.
- `.workbuddy/`, `.github/`, and `.claude/` are local-only and ignored.
- After meaningful code, architecture, build, test, or documentation changes, update `AGENTS.md` and `docs/Android_APM_项目文档.md`.
- Update `README.md` when user-facing capabilities or integration requirements change.
- Update the corresponding `docs/architecture/*.md` whenever module behavior changes.
- Keep volatile verification evidence in the project/handoff documents, not in `CLAUDE.md`.

## Required Finish Checks

1. `git status --short --branch`
2. Relevant Gradle tests/builds under JDK 17
3. For a full client CI refresh, `python tools/verify_ci.py`; otherwise run `python docs/verify_docs.py` plus documentation stale-claim checks
4. `git diff --check`
5. After push: exact equality among `HEAD`, `origin/develop`, and `git ls-remote origin refs/heads/develop`
