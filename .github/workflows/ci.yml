name: CI

# develop 为日常开发分支；main/master 为发布分支，两者均需 CI 门禁
on:
  push:
    branches: [ develop, main, master ]
  pull_request:
    branches: [ develop, main, master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Cache Gradle packages
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: gradle-

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build
        run: ./gradlew assembleDebug

      - name: Unit tests
        run: ./gradlew testDebugUnitTest

      - name: Lint
        run: ./gradlew lintDebug

      - name: Release build
        run: ./gradlew assembleRelease

      - name: Gradle plugin tests
        run: ./gradlew -p apm-plugin test

      - name: Publish SDK to Maven Local
        run: ./gradlew publishToMavenLocal

      - name: Verify published consumer
        run: ./gradlew -p smoke-tests/maven-consumer assembleDebug

      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-results
          path: '**/build/test-results/**/TEST-*.xml'
          retention-days: 14

      # lint 报告随构建产出，失败排查时无需本地重跑
      - name: Upload lint reports
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: lint-reports
          path: '**/build/reports/lint-results-*.html'
          retention-days: 14
